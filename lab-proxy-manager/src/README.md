# Сборщик прокси. Proxy .NET

## How to deploy
Скопировать пример .env
```bash
cp .env.example .env
```
> После копирования необходимо изменить секреты в .env

Далее необходимо запустить через Docker Compose
```bash
sudo docker compose up -d --build
```
